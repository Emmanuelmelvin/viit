export const VIIT_DIRECTORY = ".viit";
export const HASH_PATTERN = /^[0-9a-f]{40}$/;
export const NAME_PATTERN = /^[A-Za-z0-9._-]+$/;
export const REF_PATTERN = /^refs\/(heads|tags)\/[A-Za-z0-9._/-]+$/;
export const MAX_TAG_DEPTH = 10;
